# This file is used by ArgoCD. CI updates image.tag after pushing a new image.
backend:
  image:
    repository: nguyenmanhha/yas-backoffice-bff
    tag: main
    pullPolicy: Always
  ingress:
    enabled: true
    host: backoffice-dev.yas.local.com
  extraEnvs:
  - name: SPRING_PROFILES_ACTIVE
    value: prod
  - name: UI_HOST
    value: http://backoffice-ui:3000
