name: backoffice service ci

on:
  push:
    paths:
      - "backoffice/**"
      - ".github/workflows/backoffice-ci.yaml"
  pull_request:
    paths:
      - "backoffice/**"
      - ".github/workflows/backoffice-ci.yaml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm ci
        working-directory: backoffice

      - name: Build
        run: npm run build
        working-directory: backoffice

      - name: Lint
        run: npm run lint
        working-directory: backoffice

      - name: Format Check
        run: npx prettier --check .
        working-directory: backoffice

      - name: Security Audit
        run: npm audit --omit=dev
        continue-on-error: true
        working-directory: backoffice

      - name: Run Tests & Generate Coverage Report
        run: npm run test:coverage
        working-directory: backoffice
      - name: Upload Coverage Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backoffice-coverage-report
          path: backoffice/coverage/
          retention-days: 7

  build:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Lowercase Repository Owner
        run: |
          echo "OWNER_LC=${OWNER,,}" >>${GITHUB_ENV}
        env:
          OWNER: '${{ github.repository_owner }}'

      - name: Build Docker Image
        uses: docker/build-push-action@v6
        with:
          context: ./backoffice
          load: true
          push: false
          tags: ghcr.io/${{ env.OWNER_LC }}/yas-backoffice:latest

      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker Image
        if: github.ref == 'refs/heads/main'
        run: docker push ghcr.io/${{ env.OWNER_LC }}/yas-backoffice:latest